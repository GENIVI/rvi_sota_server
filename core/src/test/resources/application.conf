database = {
  properties = {
    user = "sota_test"
    user = ${?CORE_DB_USER}
    password = "s0ta"
    password = ${?CORE_DB_PASSWORD}
  }
  catalog = "sota_core_test"
}

messaging {
  mode = "test"
}

packages.versionFormat = ".+"
