messaging {
  kinesis {
    appName = "sota-common-messaging"
    regionName = "eu-central-1"
    streamNameSuffix = "test"
  }
}

aws {
  accessKeyId = ${AWS_ACCESS_KEY_ID}
  secretAccessKey = ${AWS_SECRET_ACCESS_KEY}
}
